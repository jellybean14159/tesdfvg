function setup() {
  var myCanvas = createCanvas(window.innerWidth,window.innerHeight);
  myCanvas.parent('processing');
  background('green');
  textSize(32);
}

function draw() {
text("kaas",100,100);

}
